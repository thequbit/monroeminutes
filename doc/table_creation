wordfrequency
	wordfrequencyid
	documentid
	word
	frequency
	
documents
	documentid
	suborganizationid
	sourceurl
	date
	name
	
organization
	organizationid
	name
	websiteurl
	
suborganization
	suborganizationid
	organizationid
	name
	websiteurl
	documentsurl
	scriptname
	
users
	userid
	username
	passwordhash
	permissionsid

permissions
	permissionsid
	canlogin
	isadmin
	enabled	
	
searches
	searchid
	keyword
	suborganizationid
	date

create database monroeminutes;

create table wordfrequency (wordfrequencyid int not null auto_increment primary key, documentid int not null, word text, frequency int);

create table documents ( documentid int not null auto_increment primary key, suborganizationid int not null, sourceurl text, date datetime, name text);

create table organization (organizationid int not null auto_increment primary key, name text, websiteurl text);

create table suborganization(suborganizationid int not null auto_increment primary key, organizationid int, name text, websiteurl text, documentsurl text, sciptname text);
	
create table users( userid int not null auto_increment primary key, displayname text not null, username text not null, passwordhash text not null, permissionsid int not null);

create table permissions( permissionsid int not null auto_increment primary key, canlogin bool not null, isadmin bool not null, enabled bool not null);

create table searches( searchid int not null auto_increment primary key, keyword text, suborganizationid int, date datetime);